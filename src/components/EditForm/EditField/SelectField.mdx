---
name: Select Field
---

import { Playground, Props } from 'docz';
import EditField from './FieldWrapper';
import Button from '@material-ui/core/Button';

# Select Field

Below is the list of build-in Selection Fileds in this library.

<Props of={EditField} />

## Standard Fields

<Playground>
  <form>
    <div style={{ margin: 10 }}>
      <EditField label="Date Picker" type="date" defaultValue={new Date()} />
    </div>
    <div style={{ margin: 10 }}>
      <EditField label="Time Picker" type="time" defaultValue={new Date()} />
    </div>
    <div style={{ margin: 10 }}>
      <EditField
        label="Date & Time Picker"
        type="datetime"
        defaultValue={new Date()}
      />
    </div>
    <div style={{ margin: 10 }}>
      <Button color="primary" variant="contained" type="submit">
        Submit
      </Button>
    </div>
  </form>
</Playground>

## Outline Fields

<Playground>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date Picker"
      variant="outlined"
      type="date"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Time Picker"
      variant="outlined"
      type="time"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date & Time Picker"
      type="datetime"
      variant="outlined"
      defaultValue={new Date()}
    />
  </div>
</Playground>

## Filled Fields

<Playground>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date Picker"
      variant="filled"
      type="date"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Time Picker"
      variant="filled"
      type="time"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date & Time Picker"
      type="datetime"
      variant="filled"
      defaultValue={new Date()}
    />
  </div>
</Playground>

## Labeled Fields

<Playground>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date Picker"
      variant="labeled"
      type="date"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Time Picker"
      variant="labeled"
      type="time"
      defaultValue={new Date()}
    />
  </div>
  <div style={{ margin: 10 }}>
    <EditField
      label="Date & Time Picker"
      type="datetime"
      variant="labeled"
      defaultValue={new Date()}
    />
  </div>
</Playground>
