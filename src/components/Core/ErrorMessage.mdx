---
name: 0. Core/Error Message
---

import { Playground, Props } from 'docz';
import ErrorMessage from './ErrorMessage';

# Error Message Component

The Core Componnet to display the Error message is using by **ErrorField** which allow to display message from Formik.

<Playground>
  <ErrorMessage>
    The Field is required. <i>(with error Icon)</i>
  </ErrorMessage>
  <br />
  <ErrorMessage messageOnly>
    The Field is required. <i>(without error Icon)</i>
  </ErrorMessage>
</Playground>

## Props

<Props of={ErrorMessage} />
